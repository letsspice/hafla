<nav class="bg-black text-white shadow px-4 py-2 flex items-center justify-between text-xs fixed top-0 left-0 right-0 z-10">
  <!-- Left: Logo -->
  <div class="flex items-center space-x-2">
   <i class="fas fa-ticket text-2xl"></i>
   <span class="text-white text-base font-bold font-montserratAlternates">
    hafla
   </span>
  </div>

  <!-- Center: Search Bar -->
  <div class="flex-1 mx-4">
    <div class="max-w-md mx-auto">
      <div class="relative">
        <input type="text" placeholder="Search..." class="w-full bg-black text-white text-xs border border-gray-300 rounded-full pl-10 pr-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500">
        <div class="absolute left-3 top-2.5 text-gray-500">
          <i class="fas fa-search"></i>
        </div>
      </div>
    </div>
  </div>

  <!-- Right: Icons -->
  <div class="flex items-center space-x-16">
    <button class="text-gray-100 hover:text-primary focus:outline-none">
      <i class="fas fa-bell text-xl"></i>
    </button>
    <%= render "layouts/navigation/profile_dropdown" %>
  </div>
</nav>